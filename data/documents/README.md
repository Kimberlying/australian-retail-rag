# Documents

Put permitted public `.pdf`, `.md`, or `.txt` documents here and rerun:

```bash
retail-rag ingest
```

The repository includes a small public-data snapshot and a clearly labelled synthetic policy so the first demo works without downloading a large report. Before using the project in a public portfolio, refresh facts from the official source URLs and keep the source metadata.
