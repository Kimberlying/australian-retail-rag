services:
  api:
    build: .
    image: australian-retail-rag:local
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      LOG_FORMAT: json
    restart: unless-stopped
